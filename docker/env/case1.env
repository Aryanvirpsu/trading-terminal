# C0 G4 — Case 1 (cloud) profile. Mirrors what the Actions workflow actually sets:
# only PAPER_DATA_DIR, nothing else (C0_INVENTORY.md §8 step 6 — Case 1 never sets a
# PAPER_*/DECISION_* tuning var). TZ=UTC because that's the runner's own timezone
# (C0_CONTAINER_DESIGN.md D1) — not fixed, preserved.
C0_PROFILE=case1
TZ=UTC
PAPER_DATA_DIR=/data/case1/paper
DASHBOARD_PORT=5057

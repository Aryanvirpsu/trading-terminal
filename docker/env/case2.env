# C0 G4 — Case 2 (local) profile. Mirrors the developer's own host: no PAPER_DATA_DIR
# (never set on the host), TZ=America/New_York (the host's own system timezone —
# C0_CONTAINER_DESIGN.md D1, not fixed, preserved).
C0_PROFILE=case2
TZ=America/New_York
DASHBOARD_PORT=5057

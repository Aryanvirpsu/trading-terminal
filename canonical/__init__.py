"""Canonical Option Architecture v1.1 — additive, non-migrated implementation.

Each module under this package implements exactly one canonical layer from the
architecture document ("Canonical Option Architecture" artifact, v1.1 corrections
section is normative). Nothing here is wired into any existing pipeline yet —
see each module's docstring for what it deliberately does NOT do.

Implemented so far:
  contract_quality  — Layer A: contract structural quality (v1.1 Step 2)
"""
